[pytest]
testpaths = tests
asyncio_mode = auto
# Starlette 1.x raises a DeprecationWarning on TestClient import (httpx vs httpx2).
# It is benign and intentional on this stack — do not let it error the suite. [C-7]
filterwarnings =
    ignore::DeprecationWarning
