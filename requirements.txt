# Runtime dependencies, pinned for reproducible images. These exact versions are
# what the test suite runs against and are verified to install and import on
# python:3.12-slim (the Dockerfile base). Bump deliberately, re-running tests.
fastapi==0.136.3
uvicorn[standard]==0.49.0
httpx==0.28.1
jinja2==3.1.6
defusedxml==0.7.1
Pillow==12.2.0
pypdf==6.16.2
