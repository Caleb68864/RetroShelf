# Research vault is versioned independently (its own git repo)
vault/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# logic-dev-kit code graph
graphify-out/

# Local cache/config volumes if created during testing
/cache/
/config/
.DS_Store

# NOTE: do NOT ignore .factory-*.txt or task-evidence/ — the dark-factory
# preflight smoke-commits .factory-preflight-smoke.txt and manages its own
# run artifacts. Ignoring them breaks the factory's commit gate (exit 63).
